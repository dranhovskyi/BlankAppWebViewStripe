<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BlankAppWebViewStripe.Controls"
             mc:Ignorable="d"
             x:Class="BlankAppWebViewStripe.Views.WebViewPage"
             Padding="0,20,0,0">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="10*" />
                <RowDefinition Height="90*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Orientation="Horizontal">
                <Button Text="Back" HorizontalOptions="StartAndExpand" Command="{Binding OnBackButtonClickedCommand}" />
            </StackLayout>
            <!-- <WebView Grid.Row="1" Source="{Binding Source}" Navigating="WebViewNavigating" /> -->
            <controls:HybridWebView x:Name="hybridWebView" Command="{Binding GetResultCommand}" Grid.Row="1" Uri="{Binding Source}" 
                HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
        </Grid>
    </ContentPage.Content>
</ContentPage>